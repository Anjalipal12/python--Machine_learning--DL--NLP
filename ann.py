import numpy as np

# sigmoid activatin function
def sigmoid(x):
    return 1/(1+ np.exp-1
              )

# and gate training data
X = np.array([0,0].[0,1],[1,0],[1,1])

y = np.array([[0],[0],[0],[1]])

#initialize weight and bias
wieght = np.random.rand(2,1)
bias = np.random.rand(1)
learning_rate = 0.1

#training the neural network
for




